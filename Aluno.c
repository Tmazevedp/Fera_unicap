#include <stdio.h>
#include <string.h>
#include "Types.h"

void inicia_arvore(TNo **arvore) {
	*arvore = NULL;
}

void cadastrar(FILE *arq, char matricula[]) {
	TAluno c;
	int flag;
	TNo *arvore_aluno;
	printf("Digite a sua matricula:\n");
	gets(c.matricula);
	fflush(stdin);
	if ()



	
}